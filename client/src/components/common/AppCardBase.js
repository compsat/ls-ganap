import styled from "styled-components";

const AppCardBase = styled.article`
  border-radius: 0.3rem;
  background-color: #fcfcfc;
  box-shadow: 0 0.1rem 0.6rem rgba(0, 0, 0, 0.16);
  cursor: pointer;
`;

export default AppCardBase;
