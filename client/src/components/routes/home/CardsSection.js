import FullWidthContainer from "components/common/FullWidthContainer";

const CardsSection = FullWidthContainer.withComponent("section").extend`
  padding-top: 4rem;
  padding-bottom: 4rem;
`;

export default CardsSection;
